function addManager() {
    // ask the managers info
    // save it
    // call mainMenu() function
  }

  function mainMenu() {
    // add an engineer, intern or build the team HTML page?
    // if you chose add an engineer then we need to call addEngineer()
    // if you chose add an intern then we need to call addIntern()
    // else buildTeam()
  }